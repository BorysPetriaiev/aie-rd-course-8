# Optional: OpenAI embeddings (costs ~$0.10 for full dataset)
# OPENAI_API_KEY=sk-...

# pgvector connection (matches docker-compose.yml defaults)
PG_DSN=postgresql://bench:bench@localhost:5432/bench

# Qdrant (matches docker-compose.yml defaults)
QDRANT_URL=http://localhost:6333
